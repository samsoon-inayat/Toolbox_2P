function [CRc,aCRc,mRR,spCR] = find_population_correlations(Rs,mR,resp,ordercol)

[CRc,aCRc,mRR] = find_population_vector_corr(Rs,mR,resp,ordercol);

for rr = 1:size(mRR,1)
    for cc1 = 1:size(mRR,2)
        thismR = mRR{rr,cc};
        spCR{rr,cc} = corr(thismR');
    end
end

n = 0;